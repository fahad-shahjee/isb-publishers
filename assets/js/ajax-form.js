// The contact form now submits as a plain server-side POST (see contact.php
// and assets/mail.php), so no JS handling is needed here. Kept as a no-op
// file since assets/js/ajax-form.js is still referenced by the shared script
// list on every page.
